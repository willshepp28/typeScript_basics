// Variable Types

let myString: string;
let myNum: number;
let myBool: boolean;
let myVar: any; // any data type
let aVoid: void;
let aNullType: null;
let isUndefined: undefined;

let strArr : string[]; // this has to be a array of strings
let numArr: number[]; // this has to be a array of numbers
let boolArr: boolean[]; // this has to be a array of booleans

let strArr2: Array<string>; // this is another way to set a string of arrays
let numArr2: Array<number>;
let boolArr2: Array<boolean>; 

let strNumTuple: [string, number]; // a array that has to be a sting and a number. Has to match exactly


myString: "Life is good";
myNum = 25;
myBool = true;

strArr = ['love', 'life', 'yolo'];
numArr = [1,2,3,4];
boolArr = [true, false, true];
aVoid = null;

console.log(myBool);
